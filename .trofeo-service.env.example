# Path is relative to repo root (WorkingDirectory in service unit).
PCAP_FILE=dzis.pcapng

# Frame index parsed from pcap.
FRAME_INDEX=0

# Stable timing profile.
ACK_TIMEOUT_MS=500
INTER_PACKET_DELAY=0.01
FRAME_DELAY=0.02
CONNECT_RETRIES=20
CONNECT_RETRY_DELAY=0.5
